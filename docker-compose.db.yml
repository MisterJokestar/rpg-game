# MongoDB only — start with: docker compose -f docker-compose.db.yml up
# Port and credentials are read from server/.env
env_file:
  - ./server/.env

services:
  mongo:
    image: docker.io/library/mongo:latest
    container_name: local-mongo
    restart: unless-stopped
    ports:
      - "${MONGO_PORT:-27017}:27017"
    env_file:
      - ./server/.env
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD:-Password4321!}
      MONGO_INITDB_DATABASE: ${MONGODB_DB_NAME:-cloud_app_dev_db}
    volumes:
      - mongo-data:/data/db
    networks:
      - app

networks:
  app:
    name: finalproj-network

volumes:
  mongo-data:
    name: finalproj-mongo-data
